#include "main.h"
/**
 * _abs - it calculate the absolute valute of an integer
 *@n: the number to find its absolute value
 *
 * Return: the absolute value of a number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

